class PPP(object):
	'A Possion Point Process, for driving Xi-coalescents'
	
	def __init__(self,intensityMeasure,n,T):
		#self.t = 0.
		self.events = {}
		#self.hasCoalesced = FALSE
		#self.lineages = n
		self.resample()

	def resample(self):
		while t <= T:
			#do stuff
	
	#def TMRCA(self):
		#if self.hasCoalesced:
			#return max(self.events.keys())
		#else return nan
